[package]
name = "spin-config"
version = "0.1.0"
edition = "2021"
authors = [ "Fermyon Engineering <engineering@fermyon.com>" ]

[dependencies]
anyhow = "1.0"
liquid = "0.24"
serde = { version = "1.0", features = [ "derive" ] }

[dev-dependencies]
toml = "0.5"